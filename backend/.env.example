DATABASE_URL=postgresql://postgres:postgres@localhost:5433/trailflow_dev
JWT_SECRET=change-me-in-production-use-64-random-chars
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
RESEND_API_KEY=re_xxx

# Email transport : "resend" (prod) ou "smtp" (dev avec MailHog)
EMAIL_TRANSPORT=smtp
SMTP_HOST=localhost
SMTP_PORT=1025
FRONTEND_URL=http://localhost:3000
PORT=3001

# Store config
STORE_NAME=TRAILFLOW
STORE_URL=https://trailflow.boutique
CONTACT_EMAIL=contact@trailflow.boutique
ORDER_PREFIX=TF

# Admin (login JWT) — change le password avant prod
ADMIN_EMAIL=admin@trailflow.boutique
ADMIN_PASSWORD=admin123
